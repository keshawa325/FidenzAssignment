Please run index.js file inside FidenzAssignment on node.JS environment to run the website on local server with port 8080:

Root page --> http://localhost:8080/

HTML(ejs) files for Dashboard and view weather web pages are inside the views folder. Respective JS files and CSS files for web pages are inside the public folder.
